node_modules/
dist/
reference/
.env.local

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Archivos de prueba / temporales (no commitear)
test_*.docx
*-pedido.json
docs/preview*.html
test-nueva-tabla.html
cronograma_regenerado.html
